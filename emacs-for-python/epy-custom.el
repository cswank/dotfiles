;; Use this file to customize your emacs for python installation!
;; uncomment what you want to enable

;; Checker configuration - choose only one of them -
;; you can also edit the command to call replacing for example the
;; "pyflakes" with other commands

;; (epy-setup-checker "pyflakes") 
;; (epy-setup-checker "python" '((concat epy-install-dir "scripts/pylint-mod.py"))
;; (epy-setup-checker "pep8")

(provide 'epy-custom)
